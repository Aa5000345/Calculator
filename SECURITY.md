# 安全政策

## 支持的版本

我们为最新发布版本提供安全更新：

| 版本 | 支持状态 |
|------|----------|
| 最新 Release | ✅ 支持 |
| 更早版本 | ❌ 不支持 |

**强烈建议始终使用[最新版本](https://github.com/Aa5000345/Calculator/releases/latest)。**

---

## 报告漏洞

**请不要通过公开 Issue 报告安全漏洞。**

如果你发现了安全漏洞，请通过以下方式之一**私下**报告：

### 方式 1：GitHub 私有漏洞报告（推荐）

1. 打开 https://github.com/Aa5000345/Calculator/security/advisories/new
2. 填写漏洞详情
3. 提交后只有维护者可见

### 方式 2：邮件

如果无法使用 GitHub，请通过仓库主页公开的联系方式联系维护者，
标题注明 `[SECURITY] Calculator`。

---

## 报告内容

请尽可能提供以下信息：

- **漏洞类型**：命令注入、路径穿越、XSS、信息泄露、权限提升等
- **受影响范围**：文件 / 函数 / 版本
- **复现步骤**：最小可复现示例（PoC）
- **潜在影响**：攻击者能做什么
- **建议的修复方案**（如有）
- **是否愿意公开致谢**（默认会公开，除非你要求匿名）

### 报告模板

```
## 漏洞类型
（例如：表达式沙箱绕过）

## 受影响版本
（例如：1.0.0 ~ 1.0.3）

## 受影响组件
（例如：core/engine.py 的 _safety_check）

## 复现步骤
1. ...
2. ...
3. 观察到 ...

## 预期行为
应该拒绝执行

## 实际行为
成功执行了危险代码

## 潜在影响
攻击者可以 ...

## 建议修复
（可选）

## 致谢方式
（实名 / 匿名）
```

---

## 响应时间

| 阶段 | 目标时间 |
|------|----------|
| 确认收到 | 7 天内 |
| 初步评估 | 14 天内 |
| 修复版本发布 | 30 天内（严重漏洞可加快） |
| 公开披露 | 修复发布后 7 天 |

如果超过 30 天仍未修复，你可以选择公开披露，我们会协助发布
安全公告。

---

## 披露政策

我们遵循**协调披露**（Coordinated Disclosure）：

1. 你私下报告漏洞
2. 我们确认并开发修复
3. 我们在 Release Notes 中致谢（除非你要求匿名）
4. 修复发布 7 天后，漏洞详情可公开讨论

**我们承诺**：

- 不会对善意报告漏洞的研究者采取法律行动
- 会在合理时间内响应
- 会在公告中致谢（除非你要求匿名）

**我们请求**：

- 给维护者合理的修复时间
- 不要在修复前公开细节
- 不要利用漏洞进行攻击或数据窃取

---

## 安全范围

### ✅ 属于安全范围

| 类别 | 具体示例 |
|------|----------|
| **表达式沙箱绕过** | `core/engine.py` 的 `_safety_check()` 被绕过，执行了 `import` / `eval` / `open` |
| **公式沙箱绕过** | `core/data_table.py` 的 AST 白名单被绕过 |
| **加密实现缺陷** | `core/crypto_tools.py` 的 AES / RSA / TOTP 实现问题 |
| **密钥泄露** | `core/secrets.py` 的密钥存储不安全 |
| **插件任意代码执行** | `core/plugins.py` 加载恶意插件时可执行任意代码 |
| **网络请求问题** | SSRF、中间人攻击、证书验证被绕过 |
| **路径穿越** | 文件操作函数可读写预期外的路径 |
| **依赖引入的漏洞** | 间接依赖被投毒（请同时报告给上游） |

### ❌ 不属于安全范围

| 类别 | 说明 |
|------|------|
| 用户输入危险表达式 | `core/engine.py` 已有沙箱，正常使用不会触发 |
| 本地权限问题 | 用户机器自身的权限配置问题 |
| 依赖库自身漏洞 | 请直接报告给上游（如 PySide6、sympy） |
| 社会工程学 | 非技术性攻击 |
| DoS（资源耗尽） | 用户输入超长表达式导致卡顿（已有长度限制） |
| 物理访问攻击 | 攻击者已能物理接触机器 |
| 用户主动运行恶意插件 | 用户需自行判断插件可信度 |

---

## 安全设计说明

MultiCalc 在设计上遵循以下安全原则：

### 1. 表达式沙箱

`core/engine.py` 的 `_safety_check()` 拒绝包含以下内容的表达式：

```python
_FORBIDDEN_SUBSTR = (
    "__", "import", "eval", "exec", "open", "lambda",
    "globals", "locals", "getattr", "setattr", "delattr",
    "compile", "input", "breakpoint", "os.", "sys.", "subprocess",
    "pickle", "marshal", "shutil", "socket",
)
```

表达式长度限制 4000 字符。

### 2. 公式沙箱

`core/data_table.py` 的 `_eval_formula` 使用 AST 白名单：

- 只允许 `BinOp` / `UnaryOp` / `Num` / `Call` 等安全节点
- `__builtins__` 被清空
- 只暴露 `abs` / `min` / `max` / `sqrt` / `log` 等安全函数

### 3. API 密钥隔离

`core/secrets.py` 将密钥存到 `~/.multicalc/secrets.json`，
与 `settings.json` **分离**：

- 导入 / 导出设置时**不会**泄露密钥
- 文件权限在 Unix 上设为 `0600`（Windows 上尽力而为）

### 4. 网络请求

- 所有外部请求都有**超时**（默认 10~20 秒）
- 失败时优雅降级到**离线数据**
- 使用 HTTPS

### 5. 插件机制

- 插件不自动执行，仅在 `register(app_context)` 时被调用
- 插件加载失败只记录日志，不影响主程序
- **用户需自行判断插件可信度**（本项目不提供插件市场）

### 6. 更新检查

- **只检查不下载**（截至 v1.0.x）
- 更新源可配置
- 未来加入下载功能时会校验 SHA256

---

## 已知限制

以下问题我们**已知但暂不修复**：

| 问题 | 原因 | 缓解措施 |
|------|------|----------|
| 表达式求值可能耗时较长 | sympy 自身限制 | 后台线程执行，可取消 |
| 首次加载 pix2tex 慢 | 模型 ~97 MB | 提示用户，异步加载 |
| 汇率 API 依赖第三方 | 免费 API 有速率限制 | 6 小时缓存 + 离线兜底 |
| 打包体积较大 | 包含 sympy / scipy / matplotlib | 未来优化 UPX / 排除子模块 |

---

## 致谢

感谢所有**负责任地**披露安全问题的研究者。修复发布后，
我们会在 Release Notes 和本文件中致谢（除非你要求匿名）。

### 历史致谢

（暂无）

---

## 联系方式

- 🔒 私有漏洞报告：https://github.com/Aa5000345/Calculator/security/advisories/new
- 🐛 一般 Bug：https://github.com/Aa5000345/Calculator/issues
- 📧 邮件：见仓库主页